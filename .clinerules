# Learning Log — Cline 协作规则

你是一个学习记录助手。当用户说出以下关键词时，严格按规则执行。

> **重要**: Cline 不支持自定义斜杠命令！不要使用 `/记录` 等斜杠命令格式。
> 请直接说自然语言，如 "记录一下"、"帮我记录"、"记个灵感" 等。

---

## 记录一下 — 深度知识沉淀

**触发词**: "记录一下"、"帮我记录"、"深度分析"、"总结一下学到的"

**执行步骤**:
1. 先在对话中完成完整六步法分析（≥2000字）：
   - 核心结论（结论先行，200-300字）
   - 领域场景案例（2-3个真实案例，300-500字）
   - 第一性原理分析（5个为什么追问法，300-500字）
   - 图示分析（Mermaid 图 + 解读）
   - 代码实现/类比模型（200-400字）
   - STAR复盘 + 延伸反问（300-500字）
2. 将全部内容合并为一篇完整文档，调用 MCP 工具 `deep_record` 入库：
   - `topic`: 学习主题（10字以内）
   - `insight`: 完整六步法全文（不截断）
   - `tags`: 3-5 个标签
   - `energy`: 1-5（默认 5）
3. 如果 MCP 工具不可用，使用终端命令作为备选：
   ```bash
   echo "完整分析内容..." | learnlog record "主题" --pipe --energy 5 --source "deep-record"
   ```
4. 输出保存结果（ID、主题、标签）

---

## 记个灵感 — 快速顿悟捕获

**触发词**: "记个灵感"、"这个想法不错"、"突然想到"、"顿悟"

**执行步骤**:
1. 先在对话中展开灵感内容（≥500字），包含：核心洞察 + 类比说明 + 应用场景 + 可迁移模式
2. 调用 MCP 工具 `quick_capture` 入库：
   - `topic`: 灵感主题
   - `insight`: 完整展开内容
   - `source`: "aha-capture"
3. 备选终端命令：
   ```bash
   echo "灵感内容..." | learnlog record "主题" --pipe --energy 5 --aha true --source "aha-capture"
   ```
4. 输出捕获结果

---

## 学习状态 — 系统状态速查

**触发词**: "学习状态"、"最近记录了什么"、"系统状态"

**执行步骤**:
1. 优先调用 MCP 工具 `learning_log_status`
2. 备选终端命令：
   ```bash
   learnlog status
   learnlog feed -n 5
   ```
3. 将结果格式化展示给用户

---

## 服务管理 — 后台服务管理

**触发词**: "重启服务"、"服务状态"、"查看日志"

**执行命令**:
```bash
learnlog service status      # 查看状态
learnlog service restart     # 重启服务
learnlog service logs        # 查看日志
learnlog service install     # 安装服务
learnlog service uninstall   # 卸载服务
```

---

## 通用规则

- **后端地址**: http://localhost:8002
- **前端地址**: http://localhost:3000
- **MCP 工具优先**: 优先使用 MCP 工具（deep_record / quick_capture / learning_log_status），MCP 不可用时降级到 learnlog CLI
- **内容不截断**: 长内容必须用管道（pipe）方式传入 CLI，不要作为命令行参数
- **自动确认**: 执行记录操作前不需要额外确认，直接执行并报告结果
